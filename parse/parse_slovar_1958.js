#!/usr/bin/env node
/**
 * Устаревшая обёртка. Актуальный парсер:
 *   npm run parse:1958 -- --dry-run
 *   npm run parse:1958 -- --direct
 */
console.error("Используйте: npm run parse:1958 -- --dry-run");
console.error("См. docs/import-1958.md");
process.exit(1);
